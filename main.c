#include <stdio.h>
#include <string.h>
#include <stdlib.h>

typedef struct{
  int line_number;
  char lex[32];
  int tok;
  int att;
} Token;

int main(int argc, char *argv[]) {
  int b = 0;
 // Token *new = get_token_relop("<=", &b);
  return 0;
}
